# Fase 1.5 — De-hardcode do `google-drive.ts` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrar os 4 clusters remanescentes de `google-drive.ts` que usam a constante global `ATRIBUICAO_FOLDER_IDS`/`FOLDER_ID_TO_ATRIBUICAO` para o resolver por grupo (`drive-folders.ts`), com fallback legado e reverse-sync multi-tenant.

**Architecture:** Adiciona um lookup reverso `folderId → {ownerUserId, atribuicao}` em `drive-folders.ts` (consulta `drive_groups.atribuicaoFolders`, a fonte única de verdade). O webhook de reverse-sync passa a atribuir `defensorId = ownerUserId` aos assistidos criados/vinculados (match de dedup permanece global). As 3 funções forward ganham um parâmetro **opcional** `ownerUserId` com fallback para a constante legada — backward-compatible, sem ripple nos ~10 chamadores.

**Tech Stack:** TypeScript (strict), Drizzle ORM (Postgres/Supabase), vitest, Next.js 15.

## Global Constraints

- **NÃO tocar em `SPECIAL_FOLDER_IDS`** (DISTRIBUICAO etc.) — usado por webhooks/upload/whatsapp/inngest, independe de grupo.
- `ownerUserId` é **opcional** nas funções forward; quando ausente ou sem grupo → fallback `ATRIBUICAO_FOLDER_IDS[atribuicao]` (comportamento single-tenant atual preservado).
- O resolver retorna a **chave de grupo** (`Atribuicao`: `"JURI" | "VVD" | "EP" | "SUBSTITUICAO" | "GRUPO_JURI" | "CRIMINAL"`); o reverse-sync converte para o **enum do banco** (`atribuicaoPrimaria`) via `ATRIBUICAO_KEY_TO_ENUM` (`JURI→JURI_CAMACARI`, `VVD→VVD_CAMACARI`, `EP→EXECUCAO_PENAL`, `SUBSTITUICAO→SUBSTITUICAO`, `GRUPO_JURI→GRUPO_JURI`, `CRIMINAL→CRIMINAL_CAMACARI`).
- Atribuição de dono: **CREATE** → `defensorId = ownerUserId`; **LINK** (assistido existente) → `defensorId = bestMatch.defensorId ?? ownerUserId` (nunca sobrescreve dono já definido).
- TDD: tarefas 1–2 com testes executáveis; tarefas 3–5 com **contract tests** (lê o source como string e asserta a fiação — padrão de `src/lib/trpc/routers/__tests__/siga-router.test.ts`), pois exercitam Drive API/DB reais.
- Gate final: `npx tsc --noEmit` + `npx vitest run` verdes.
- Worktree: `Defender-wt-drive-multitenant`, branch `feat/drive-multi-tenant`. `node_modules` é symlink do checkout principal.

## File Structure

| Arquivo | Responsabilidade | Ação |
|---|---|---|
| `src/lib/services/drive-folders.ts` | Resolver por grupo (forward + **reverso**) | Modificar (add 2 funções) |
| `src/lib/services/__tests__/drive-folders.test.ts` | Testes do resolver | Modificar (add casos) |
| `src/lib/services/google-drive.ts` | Operações Drive (4 clusters) | Modificar |
| `src/lib/services/__tests__/google-drive-fase15.test.ts` | Contract tests da fiação | Criar |
| `src/lib/inngest/functions.ts` | Chamador de `isAtribuicaoRootChild` | Modificar (1 linha: `await`) |
| `src/lib/trpc/routers/assistidos.ts` | Chamador canônico owner-aware | Modificar (`ensureDriveFolderForAssistido`) |

---

### Task 1: Lookup reverso puro `findAtribuicaoForFolder`

**Files:**
- Modify: `src/lib/services/drive-folders.ts`
- Test: `src/lib/services/__tests__/drive-folders.test.ts`

**Interfaces:**
- Consumes: `AtribuicaoFoldersMap`, `Atribuicao`, `pickAtribuicaoFolders` (já existem no arquivo).
- Produces: `export function findAtribuicaoForFolder(folders: AtribuicaoFoldersMap, folderId: string): Atribuicao | null`

- [ ] **Step 1: Write the failing test**

Adicionar ao final de `src/lib/services/__tests__/drive-folders.test.ts`:

```ts
import { findAtribuicaoForFolder } from "../drive-folders";

describe("findAtribuicaoForFolder (lookup reverso puro)", () => {
  const folders = { JURI: ["fJuri"], VVD: ["fVvdCrim", "fVvdMpu"], EP: ["fEp"] };

  it("acha a atribuição cuja lista contém o folderId", () => {
    expect(findAtribuicaoForFolder(folders, "fVvdMpu")).toBe("VVD");
    expect(findAtribuicaoForFolder(folders, "fJuri")).toBe("JURI");
  });

  it("retorna null quando nenhum grupo contém o folderId", () => {
    expect(findAtribuicaoForFolder(folders, "desconhecido")).toBeNull();
  });

  it("tolera valor string legado no mapa", () => {
    expect(findAtribuicaoForFolder({ EP: "fEpLegado" } as any, "fEpLegado")).toBe("EP");
  });

  it("retorna null para mapa vazio", () => {
    expect(findAtribuicaoForFolder({}, "qualquer")).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/services/__tests__/drive-folders.test.ts`
Expected: FAIL — `findAtribuicaoForFolder is not a function` / import error.

- [ ] **Step 3: Write minimal implementation**

Adicionar em `src/lib/services/drive-folders.ts`, logo após `pickAtribuicaoFolderPrimary`:

```ts
/** Pura: varre o mapa de um grupo procurando o folderId → sua chave de atribuição. */
export function findAtribuicaoForFolder(
  folders: AtribuicaoFoldersMap,
  folderId: string,
): Atribuicao | null {
  for (const atribuicao of ATRIBUICOES) {
    if (pickAtribuicaoFolders(folders, atribuicao).includes(folderId)) {
      return atribuicao;
    }
  }
  return null;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/lib/services/__tests__/drive-folders.test.ts`
Expected: PASS (todos os casos novos + os 6 existentes).

- [ ] **Step 5: Commit**

```bash
git add src/lib/services/drive-folders.ts src/lib/services/__tests__/drive-folders.test.ts
git commit -m "feat(drive): findAtribuicaoForFolder — lookup reverso puro folderId->atribuicao (TDD)"
```

---

### Task 2: Lookup reverso async `resolveFolderToAtribuicao`

**Files:**
- Modify: `src/lib/services/drive-folders.ts`
- Test: `src/lib/services/__tests__/drive-folders-reverse.test.ts` (Create)

**Interfaces:**
- Consumes: `findAtribuicaoForFolder` (Task 1), `driveGroups` (schema), `db`.
- Produces: `export async function resolveFolderToAtribuicao(folderId: string): Promise<{ ownerUserId: number; driveGroupId: number; atribuicao: Atribuicao } | null>`

- [ ] **Step 1: Write the failing test**

Criar `src/lib/services/__tests__/drive-folders-reverse.test.ts`:

```ts
import { vi, describe, it, expect, beforeEach } from "vitest";

const groupsRef: { rows: any[] } = { rows: [] };

vi.mock("@/lib/db", () => ({
  db: {
    query: {
      driveGroups: {
        findMany: async () => groupsRef.rows,
      },
    },
  },
}));

import { resolveFolderToAtribuicao } from "../drive-folders";

describe("resolveFolderToAtribuicao (async, sem sessão)", () => {
  beforeEach(() => {
    groupsRef.rows = [
      { id: 10, ownerUserId: 1, atribuicaoFolders: { VVD: ["fVvd"], JURI: ["fJuri"] } },
      { id: 20, ownerUserId: 2, atribuicaoFolders: { EP: ["fEp2"] } },
    ];
  });

  it("resolve o grupo e a atribuição dono do folderId", async () => {
    expect(await resolveFolderToAtribuicao("fJuri")).toEqual({
      ownerUserId: 1, driveGroupId: 10, atribuicao: "JURI",
    });
    expect(await resolveFolderToAtribuicao("fEp2")).toEqual({
      ownerUserId: 2, driveGroupId: 20, atribuicao: "EP",
    });
  });

  it("retorna null quando nenhum grupo contém o folder", async () => {
    expect(await resolveFolderToAtribuicao("inexistente")).toBeNull();
  });

  it("retorna null quando não há grupos", async () => {
    groupsRef.rows = [];
    expect(await resolveFolderToAtribuicao("fJuri")).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/services/__tests__/drive-folders-reverse.test.ts`
Expected: FAIL — `resolveFolderToAtribuicao is not a function`.

- [ ] **Step 3: Write minimal implementation**

Adicionar em `src/lib/services/drive-folders.ts`, ao final (após `resolveAllAtribuicaoFolders`):

```ts
/** Lookup reverso: folderId → {dono, grupo, atribuição}, sem sessão. Varre todos os grupos. */
export async function resolveFolderToAtribuicao(
  folderId: string,
): Promise<{ ownerUserId: number; driveGroupId: number; atribuicao: Atribuicao } | null> {
  const groups = await db.query.driveGroups.findMany({
    columns: { id: true, ownerUserId: true, atribuicaoFolders: true },
  });
  for (const g of groups) {
    const atribuicao = findAtribuicaoForFolder(
      (g.atribuicaoFolders ?? {}) as AtribuicaoFoldersMap,
      folderId,
    );
    if (atribuicao) {
      return { ownerUserId: g.ownerUserId, driveGroupId: g.id, atribuicao };
    }
  }
  return null;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/lib/services/__tests__/drive-folders-reverse.test.ts`
Expected: PASS (3 casos).

- [ ] **Step 5: Commit**

```bash
git add src/lib/services/drive-folders.ts src/lib/services/__tests__/drive-folders-reverse.test.ts
git commit -m "feat(drive): resolveFolderToAtribuicao — lookup reverso por grupo (db mock, TDD)"
```

---

### Task 3: Reverse-sync multi-tenant em `google-drive.ts`

**Files:**
- Modify: `src/lib/services/google-drive.ts` (cluster reverso: `FOLDER_ID_TO_ATRIBUICAO` ~L4222, `handleNewAssistidoFolder` ~L4254, `isAtribuicaoRootChild` ~L4413)
- Modify: `src/lib/inngest/functions.ts:1034` (add `await`)
- Test: `src/lib/services/__tests__/google-drive-fase15.test.ts` (Create)

**Interfaces:**
- Consumes: `resolveFolderToAtribuicao`, `type Atribuicao` (Task 2).
- Produces: `export async function isAtribuicaoRootChild(parentDriveId: string | undefined): Promise<boolean>`; `handleNewAssistidoFolder` mantém a mesma assinatura `(folderId, folderName, parentFolderId)`.

- [ ] **Step 1: Write the failing contract test**

Criar `src/lib/services/__tests__/google-drive-fase15.test.ts`:

```ts
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { describe, it, expect } from "vitest";

const ROOT = process.cwd();
const gdrive = readFileSync(join(ROOT, "src/lib/services/google-drive.ts"), "utf8");
const inngest = readFileSync(join(ROOT, "src/lib/inngest/functions.ts"), "utf8");

describe("Fase 1.5 — reverse-sync multi-tenant (contract)", () => {
  it("removeu o mapa global FOLDER_ID_TO_ATRIBUICAO", () => {
    expect(gdrive).not.toContain("FOLDER_ID_TO_ATRIBUICAO");
  });
  it("usa o resolver reverso por grupo", () => {
    expect(gdrive).toContain("resolveFolderToAtribuicao");
  });
  it("isAtribuicaoRootChild virou async", () => {
    expect(gdrive).toMatch(/export async function isAtribuicaoRootChild/);
  });
  it("atribui defensorId = ownerUserId nos inserts do reverse-sync", () => {
    expect(gdrive).toContain("ATRIBUICAO_KEY_TO_ENUM");
    expect(gdrive).toMatch(/defensorId:\s*ownerUserId/);
  });
  it("inngest aguarda isAtribuicaoRootChild", () => {
    expect(inngest).toMatch(/await isAtribuicaoRootChild\(/);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/services/__tests__/google-drive-fase15.test.ts`
Expected: FAIL (ainda contém `FOLDER_ID_TO_ATRIBUICAO`, função síncrona).

- [ ] **Step 3a: Adicionar imports e o mapa key→enum**

Em `src/lib/services/google-drive.ts`, na linha de import do resolver (perto do topo, junto aos outros imports de serviço), adicionar:

```ts
import { resolveFolderToAtribuicao, type Atribuicao } from "./drive-folders";
```

E logo acima de `handleNewAssistidoFolder` (substituindo o bloco `const FOLDER_ID_TO_ATRIBUICAO = ...`), inserir:

```ts
/** Chave de grupo (resolver) → valor do enum atribuicaoPrimaria do banco. */
const ATRIBUICAO_KEY_TO_ENUM: Record<Atribuicao, string> = {
  JURI: "JURI_CAMACARI",
  VVD: "VVD_CAMACARI",
  EP: "EXECUCAO_PENAL",
  SUBSTITUICAO: "SUBSTITUICAO",
  GRUPO_JURI: "GRUPO_JURI",
  CRIMINAL: "CRIMINAL_CAMACARI",
};
```

(Remover por completo o antigo `const FOLDER_ID_TO_ATRIBUICAO: Record<string, string> = Object.fromEntries(... );`.)

- [ ] **Step 3b: Reescrever o topo de `handleNewAssistidoFolder`**

Substituir:

```ts
  // 1. Check if parent is an atribuição root folder
  const atribuicao = FOLDER_ID_TO_ATRIBUICAO[parentFolderId];
  if (!atribuicao) {
    return null; // Not a direct child of an atribuição folder — skip
  }
```

por:

```ts
  // 1. Resolve atribuição + dono a partir do parent (multi-tenant, sem sessão)
  const resolved = await resolveFolderToAtribuicao(parentFolderId);
  if (!resolved) {
    return null; // Not a direct child of an atribuição folder — skip
  }
  const { ownerUserId } = resolved;
  const atribuicao = ATRIBUICAO_KEY_TO_ENUM[resolved.atribuicao];
```

- [ ] **Step 3c: Atribuir dono no LINK e nos CREATE**

(i) No `select` de `allAssistidos`, adicionar a coluna `defensorId`:

```ts
  const allAssistidos = await db
    .select({ id: assistidos.id, nome: assistidos.nome, driveFolderId: assistidos.driveFolderId, defensorId: assistidos.defensorId })
    .from(assistidos)
    .where(isNull(assistidos.deletedAt));
```

(ii) No objeto `bestMatch`, propagar `defensorId`:

```ts
      bestMatch = {
        id: a.id,
        nome: a.nome,
        confidence: similarity,
        hasFolder: !!a.driveFolderId,
        defensorId: a.defensorId,
      };
```

E ampliar o tipo da declaração de `bestMatch`:

```ts
  let bestMatch: { id: number; nome: string; confidence: number; hasFolder: boolean; defensorId: number | null } | null = null;
```

(iii) No update do LINK (ramo `confidence >= 0.85`, `!bestMatch.hasFolder`):

```ts
      await db
        .update(assistidos)
        .set({ driveFolderId: folderId, defensorId: bestMatch.defensorId ?? ownerUserId, updatedAt: new Date() })
        .where(eq(assistidos.id, bestMatch.id));
```

(iv) Nos dois `db.insert(assistidos).values({...})` (ramos `created_pending` e `created`), adicionar `defensorId: ownerUserId,` ao objeto de valores. Exemplo (ramo `created`):

```ts
  const [created] = await db.insert(assistidos).values({
    nome: titleCaseName,
    atribuicaoPrimaria: atribuicao as typeof assistidos.$inferInsert.atribuicaoPrimaria,
    driveFolderId: folderId,
    origemCadastro: "drive_sync",
    defensorId: ownerUserId,
  }).returning({ id: assistidos.id });
```

(idêntico no ramo `created_pending`, mantendo o campo `duplicataSugerida`.)

- [ ] **Step 3d: Tornar `isAtribuicaoRootChild` async**

Substituir a função inteira por:

```ts
/**
 * Checks if a Drive file/folder is a direct child of an atribuição root folder.
 * Used by syncIncremental to detect new assistido folders.
 */
export async function isAtribuicaoRootChild(parentDriveId: string | undefined): Promise<boolean> {
  if (!parentDriveId) return false;
  return (await resolveFolderToAtribuicao(parentDriveId)) !== null;
}
```

- [ ] **Step 3e: Atualizar o chamador no inngest**

Em `src/lib/inngest/functions.ts:1034`, trocar:

```ts
          if (!parentId || !isAtribuicaoRootChild(parentId)) continue;
```

por:

```ts
          if (!parentId || !(await isAtribuicaoRootChild(parentId))) continue;
```

- [ ] **Step 4: Run test + typecheck**

Run: `npx vitest run src/lib/services/__tests__/google-drive-fase15.test.ts && npx tsc --noEmit`
Expected: testes PASS; `tsc` exit 0.

- [ ] **Step 5: Commit**

```bash
git add src/lib/services/google-drive.ts src/lib/inngest/functions.ts src/lib/services/__tests__/google-drive-fase15.test.ts
git commit -m "feat(drive): reverse-sync multi-tenant — resolve dono por grupo + atribui defensorId (Fase 1.5)"
```

---

### Task 4: De-hardcode forward — helper + `ownerUserId` opcional

**Files:**
- Modify: `src/lib/services/google-drive.ts` (`createOrFindAssistidoFolder` ~L3498, `moveAssistidoFolder` ~L3524, `listAssistidoFoldersWithCount` ~L3884)
- Test: `src/lib/services/__tests__/google-drive-fase15.test.ts` (append)

**Interfaces:**
- Consumes: `resolveAtribuicaoFolder`, `type Atribuicao` (drive-folders), `ATRIBUICAO_FOLDER_IDS`.
- Produces:
  - `folderForAtribuicaoOrLegacy(ownerUserId: number | null | undefined, atribuicao: Atribuicao): Promise<string | null>`
  - `createOrFindAssistidoFolder(atribuicao, nomeAssistido, ownerUserId?: number | null)`
  - `moveAssistidoFolder(folderId, oldAtribuicao, newAtribuicao, ownerUserId?: number | null)`
  - `listAssistidoFoldersWithCount(atribuicao, ownerUserId?: number | null)`

- [ ] **Step 1: Write the failing contract test**

Adicionar ao `src/lib/services/__tests__/google-drive-fase15.test.ts`:

```ts
describe("Fase 1.5 — de-hardcode forward (contract)", () => {
  it("define o helper de fallback legado", () => {
    expect(gdrive).toContain("folderForAtribuicaoOrLegacy");
  });
  it("threada ownerUserId nas 3 assinaturas forward", () => {
    expect(gdrive).toMatch(/createOrFindAssistidoFolder\([^)]*ownerUserId/s);
    expect(gdrive).toMatch(/moveAssistidoFolder\([^)]*ownerUserId/s);
    expect(gdrive).toMatch(/listAssistidoFoldersWithCount\([^)]*ownerUserId/s);
  });
  it("as funções forward obtêm a pasta via o helper (>=4 ocorrências: def + create + move x2 + list)", () => {
    const usos = gdrive.match(/folderForAtribuicaoOrLegacy\(/g) ?? [];
    expect(usos.length).toBeGreaterThanOrEqual(4);
  });
  it("não há mais indexação direta ATRIBUICAO_FOLDER_IDS[...] (vai toda pelo helper)", () => {
    expect(gdrive).not.toMatch(/ATRIBUICAO_FOLDER_IDS\[/);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/services/__tests__/google-drive-fase15.test.ts`
Expected: FAIL (helper inexistente; ainda há índices diretos `ATRIBUICAO_FOLDER_IDS[`).

- [ ] **Step 3a: Adicionar import e helper de fallback**

Atualizar o import do resolver (Task 3 já importou `resolveFolderToAtribuicao` e `Atribuicao`) para incluir `resolveAtribuicaoFolder`:

```ts
import { resolveAtribuicaoFolder, resolveFolderToAtribuicao, type Atribuicao } from "./drive-folders";
```

Adicionar o helper (perto do topo da seção de pastas de assistido, antes de `createOrFindAssistidoFolder`):

```ts
/** Pasta da atribuição pelo grupo do dono, ou constante legada (grupo padrão legado). */
async function folderForAtribuicaoOrLegacy(
  ownerUserId: number | null | undefined,
  atribuicao: Atribuicao,
): Promise<string | null> {
  if (ownerUserId != null) {
    const resolved = await resolveAtribuicaoFolder(ownerUserId, atribuicao);
    if (resolved) return resolved;
  }
  const legacy = ATRIBUICAO_FOLDER_IDS as Record<string, string>;
  return legacy[atribuicao] ?? null;
}
```

- [ ] **Step 3b: `createOrFindAssistidoFolder`**

Trocar a assinatura e a obtenção do parent:

```ts
export async function createOrFindAssistidoFolder(
  atribuicao: "JURI" | "VVD" | "EP" | "SUBSTITUICAO" | "GRUPO_JURI" | "CRIMINAL",
  nomeAssistido: string,
  ownerUserId?: number | null,
): Promise<DriveFolder | null> {
  const parentFolderId = await folderForAtribuicaoOrLegacy(ownerUserId, atribuicao);
  if (!parentFolderId) return null;
  const nomePasta = toTitleCase(nomeAssistido);

  const existente = await searchFolderByName(nomePasta, parentFolderId);
  if (existente) {
    return existente;
  }
  return await createFolder(nomePasta, parentFolderId);
}
```

- [ ] **Step 3c: `moveAssistidoFolder`**

```ts
export async function moveAssistidoFolder(
  folderId: string,
  oldAtribuicao: "JURI" | "VVD" | "EP" | "SUBSTITUICAO" | "GRUPO_JURI",
  newAtribuicao: "JURI" | "VVD" | "EP" | "SUBSTITUICAO" | "GRUPO_JURI",
  ownerUserId?: number | null,
): Promise<{ success: boolean; error?: string }> {
  if (oldAtribuicao === newAtribuicao) {
    return { success: true };
  }

  const oldParentId = await folderForAtribuicaoOrLegacy(ownerUserId, oldAtribuicao);
  const newParentId = await folderForAtribuicaoOrLegacy(ownerUserId, newAtribuicao);

  if (!oldParentId || !newParentId) {
    return { success: false, error: "Pasta raiz da atribuição não configurada" };
  }

  const result = await moveFileInDrive(folderId, newParentId, oldParentId);
  if (!result) {
    return { success: false, error: "Erro ao mover pasta no Drive" };
  }

  return { success: true };
}
```

- [ ] **Step 3d: `listAssistidoFoldersWithCount`**

```ts
export async function listAssistidoFoldersWithCount(
  atribuicao: "JURI" | "VVD" | "EP" | "SUBSTITUICAO" | "GRUPO_JURI",
  ownerUserId?: number | null,
): Promise<Array<DriveFolder & { processoCount: number }>> {
  const parentFolderId = await folderForAtribuicaoOrLegacy(ownerUserId, atribuicao);
  if (!parentFolderId) return [];
  const folders = await listSubfolders(parentFolderId);

  const foldersWithCount = await Promise.all(
    folders.map(async (folder) => {
      const subfolders = await listSubfolders(folder.id);
      return {
        ...folder,
        processoCount: subfolders.length,
      };
    })
  );

  return foldersWithCount;
}
```

- [ ] **Step 4: Run test + typecheck**

Run: `npx vitest run src/lib/services/__tests__/google-drive-fase15.test.ts && npx tsc --noEmit`
Expected: PASS; `tsc` exit 0 (os ~10 chamadores de `createOrFindAssistidoFolder` continuam válidos — param é opcional).

- [ ] **Step 5: Commit**

```bash
git add src/lib/services/google-drive.ts src/lib/services/__tests__/google-drive-fase15.test.ts
git commit -m "feat(drive): forward de-hardcode — ownerUserId opcional + fallback legado nas 3 funções (Fase 1.5)"
```

---

### Task 5: Chamador canônico owner-aware em `assistidos.ts`

**Files:**
- Modify: `src/lib/trpc/routers/assistidos.ts` (`ensureDriveFolderForAssistido` ~L11-58)
- Test: `src/lib/trpc/routers/__tests__/assistidos-drive-owner.test.ts` (Create)

**Interfaces:**
- Consumes: `createOrFindAssistidoFolder`, `moveAssistidoFolder` (com `ownerUserId` opcional — Task 4); `assistidos.defensorId`.
- Produces: nenhuma assinatura pública nova — `ensureDriveFolderForAssistido` resolve o dono internamente a partir do `assistidoId`.

- [ ] **Step 1: Write the failing contract test**

Criar `src/lib/trpc/routers/__tests__/assistidos-drive-owner.test.ts`:

```ts
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { describe, it, expect } from "vitest";

const src = readFileSync(join(process.cwd(), "src/lib/trpc/routers/assistidos.ts"), "utf8");

describe("assistidos — owner-aware drive (contract)", () => {
  it("resolve o defensorId do assistido como dono", () => {
    expect(src).toMatch(/defensorId:\s*assistidos\.defensorId/);
    expect(src).toContain("ownerUserId");
  });
  it("passa ownerUserId para createOrFindAssistidoFolder e moveAssistidoFolder", () => {
    expect(src).toMatch(/createOrFindAssistidoFolder\(\s*folderKey,\s*nome,\s*ownerUserId/);
    expect(src).toMatch(/moveAssistidoFolder\(\s*existingFolderId,\s*oldKey,\s*folderKey,\s*ownerUserId/);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/trpc/routers/__tests__/assistidos-drive-owner.test.ts`
Expected: FAIL (sem `ownerUserId` no wrapper).

- [ ] **Step 3: Threadar o dono dentro de `ensureDriveFolderForAssistido`**

Em `src/lib/trpc/routers/assistidos.ts`, dentro de `ensureDriveFolderForAssistido`, logo após `if (!folderKey) return;`, inserir a resolução do dono:

```ts
    // Dono = defensor responsável do assistido (null → fallback grupo legado)
    const [ownerRow] = await db
      .select({ defensorId: assistidos.defensorId })
      .from(assistidos)
      .where(eq(assistidos.id, assistidoId))
      .limit(1);
    const ownerUserId = ownerRow?.defensorId ?? null;
```

Passar `ownerUserId` nas duas chamadas:

```ts
        const moveResult = await moveAssistidoFolder(existingFolderId, oldKey, folderKey, ownerUserId);
```

```ts
    const folder = await createOrFindAssistidoFolder(folderKey, nome, ownerUserId);
```

- [ ] **Step 4: Run test + typecheck**

Run: `npx vitest run src/lib/trpc/routers/__tests__/assistidos-drive-owner.test.ts && npx tsc --noEmit`
Expected: PASS; `tsc` exit 0.

- [ ] **Step 5: Commit**

```bash
git add src/lib/trpc/routers/assistidos.ts src/lib/trpc/routers/__tests__/assistidos-drive-owner.test.ts
git commit -m "feat(drive): assistidos.ts thread defensorId como dono nas operacoes de pasta (Fase 1.5)"
```

---

### Task 6: Gate final + nota no spec

**Files:**
- Modify: `docs/superpowers/specs/2026-06-29-fase1.5-google-drive-de-hardcode-design.md` (marcar concluído)

- [ ] **Step 1: Suite completa**

Run: `npx vitest run`
Expected: todos os testes PASS (incluindo os 3 arquivos novos da Fase 1.5 e os de Fase 1).

- [ ] **Step 2: Typecheck completo**

Run: `npx tsc --noEmit`
Expected: exit 0, sem erros.

- [ ] **Step 3: Marcar o spec como implementado**

Adicionar ao final de `docs/superpowers/specs/2026-06-29-fase1.5-google-drive-de-hardcode-design.md`:

```markdown
## Status

Implementado em `feat/drive-multi-tenant` (Fase 1.5). Gate verde: `tsc --noEmit` + `vitest run`.
Infra pendente (usuário): `db:push` (0066), seed do grupo, onboarding OAuth de 2º defensor.
```

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/specs/2026-06-29-fase1.5-google-drive-de-hardcode-design.md
git commit -m "docs(spec): marca Fase 1.5 como implementada (gate verde)"
```

---

## Self-Review (preenchido pelo autor do plano)

**Spec coverage:**
- Unidade 1 (resolução reversa pura + async) → Tasks 1, 2. ✓
- Unidade 2 (fallback legado) → Task 4 (`folderForAtribuicaoOrLegacy`). ✓
- Unidade 3 (reverse-sync webhook + owner attribution + async guard) → Task 3. ✓
- Unidade 4 (de-hardcode forward 3 sites) → Task 4; chamador canônico → Task 5. ✓
- Testes/Gate → Tasks 1–6. ✓
- `SPECIAL_FOLDER_IDS` intocado → garantido pelos Global Constraints (nenhuma task o referencia). ✓

**Placeholder scan:** sem TBD/TODO; todo passo de código mostra o código. ✓

**Type consistency:** `Atribuicao`, `AtribuicaoFoldersMap`, `findAtribuicaoForFolder`, `resolveFolderToAtribuicao`, `folderForAtribuicaoOrLegacy`, `ownerUserId`, `ATRIBUICAO_KEY_TO_ENUM` usados de forma idêntica entre tasks. `resolveFolderToAtribuicao` retorna `{ownerUserId, driveGroupId, atribuicao}` (Task 2) e é consumido assim na Task 3. ✓
