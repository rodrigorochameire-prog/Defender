# SEEU Fase 1.5 — Pendências de Incidentes

> **Status:** design aprovado (brainstorming 2026-07-01). Build enxuto (edge-case raro).
> **Contexto:** completa o módulo de importação SEEU (Fase 1). As abas Manifestação/Ciência/Razões já são raspadas; falta a aba **Pendências de Incidentes**, deixada para 1.5 por ter fluxo próprio.

## 1. Objetivo

A aba "Pendências de Incidentes" da Mesa do Defensor (SEEU) tem fluxo diferente: exige selecionar um **Juízo** num `<select>` antes de carregar os itens, e a tabela tem formato distinto (não segue o padrão `Seq→CNJ` das outras abas). **Quase sempre está vazia.** Quando aparecer algo, deve **entrar na triagem** (`seeu_import_staging` → `5_TRIAGEM`) e ser analisado como as intimações — para que nenhum incidente pendente passe despercebido.

**Princípio central:** *nunca perder silenciosamente* um item. Como o layout do caso populado é raro e ainda não observado, o parse é **defensivo**: tenta o padrão conhecido; se não casar mas houver conteúdo, captura cru e roteia para triagem como "verificar".

### Escopo (fechado)

| Decisão | Valor |
|---|---|
| Aba | "Pendências de Incidentes" (só ela; as outras 3 já existem) |
| Fluxo | clica aba → itera o `<select>` de Juízo → por juízo, lê a tabela |
| Caso usual (vazio) | no-op, só loga `0 pendências em <juízo>` |
| Caso com itens | captura → `seeu_import_staging` → `5_TRIAGEM`, ato "Pendência de incidente — verificar" |
| Parse | defensivo: tenta `Seq→CNJ`; senão, captura cru (não perde) |
| Read-only | nunca clica ação (igual Fase 1) |

## 2. Arquitetura

Estende `_async_scrape_mesa` em `.claude/skills/seeu-intimacoes-import/scripts/seeu_intimacoes_import.py` com um caminho especial para a aba `pendencias` (as outras abas seguem o loop atual de SITUACOES intacto).

```
para a aba "pendencias":
  clica a aba (JS_CLICK_ABA "Pendências de Incidentes")
  descobre o <select> de Juízo (JS: querySelector no frame) → lista as options
  para cada option (juízo):
     seleciona a option + dispara onchange (JS) → espera → re-resolve frame
     lê JS_TABLE_TEXT
     itens = parse_pendencias(texto)         # defensivo (função pura, testada)
     para cada item: results.append({aba:"pendencias", ato:"Pendência de incidente — verificar",
                                     processoNumero, seq, conteudo:"Mesa do Defensor\n"+bloco})
  (vazio → nenhum append; só status_cb log)
```

Reusa toda a maquinaria da Fase 1 a jusante: `results` → o mesmo caminho de staging (dedup processo+Seq, senão content_hash) → `5_TRIAGEM` (parser TS `parseIntimacoesUnificado` interpreta o `conteudo`; a sentinela "Mesa do Defensor" garante roteamento SEEU).

## 3. Componentes

| Arquivo | Papel |
|---|---|
| `.claude/skills/seeu-intimacoes-import/scripts/seeu_intimacoes_import.py` (modify) | +entrada `pendencias` em `ABAS_SUPORTADAS`; +`parse_pendencias` (puro); +caminho especial no `_async_scrape_mesa` (iteração do `<select>` de Juízo) |
| `.claude/skills/seeu-intimacoes-import/scripts/test_pendencias.py` (novo) | pytest do parse defensivo |

### 3.1 Função pura `parse_pendencias(texto: str) -> list[tuple[int|None, str|None, str]]`

Retorna lista de `(seq, cnj, bloco)`:
1. tenta `_split_blocos_por_processo(texto)` (padrão Seq→CNJ); se achar ≥1, retorna esses.
2. senão, se a tabela **tem conteúdo** (não vazia e não casa `_TABELA_VAZIA_RE = /nenhum registro|não há|sem pend[êe]ncias/i`): retorna **um** item cru `(None, None, texto_limpo)` — captura defensiva (não perde).
3. senão (vazia): retorna `[]`.

Dedup: item cru sem seq/cnj → cai no `content_hash` da Fase 1 (já suportado). Item com Seq→CNJ → dedup normal processo+Seq.

### 3.2 Iteração do `<select>` de Juízo (browser, live)

JS defensivo que **descobre** o select (sem depender do id exato): `JS_LIST_JUIZOS` = enumera `frame.querySelectorAll('select')` relevantes → devolve as options (value+label); `JS_SELECT_JUIZO(value)` = seta o value + dispara `change`/`onchange`. Se nenhum `<select>` for encontrado na aba, loga e trata a aba como vazia (defensivo).

## 4. Erros & robustez

- Aba/juízo falha (frame desanexado) → loga, pula aquele juízo, segue os demais (igual à resiliência por-aba atual).
- Nenhum `<select>` de Juízo → trata como vazia (não quebra).
- `parse_pendencias` nunca lança em texto malformado.
- Read-only; nunca clica ação; nunca escreve no SEEU.
- Idempotente: dedup da Fase 1 evita reimportar o mesmo item.

## 5. Testes

- **pytest (puro):** `parse_pendencias` — (a) texto com padrão Seq→CNJ → retorna os blocos; (b) texto com conteúdo fora do padrão → 1 item cru; (c) texto de tabela vazia ("nenhum registro") → `[]`; (d) texto vazio → `[]`.
- **Aceite ao vivo (precisa do SEEU aberto):** confirmar (a) a aba é aberta e o `<select>` de Juízo é iterado; (b) caso vazio = no-op limpo (log `0 pendências`); (c) [quando surgir item real] cai em `5_TRIAGEM`. Como é raro, o caso populado refina o parse no primeiro item real.

## 6. Fora de escopo

- Paginação dentro da aba (as outras abas também não paginam; há aviso de under-capture se surgir).
- UI dedicada (itens entram na triagem existente, sem surface nova).

## 7. Riscos & mitigações

| Risco | Mitigação |
|---|---|
| Layout populado desconhecido (raro) | Parse defensivo: captura cru se não casar o padrão → nada some; refina no 1º real |
| `<select>` de Juízo com id/estrutura variável | Descoberta genérica via `querySelectorAll('select')`, não id fixo |
| Regressão nas abas existentes | Caminho especial só para `pendencias`; o loop das outras abas fica intacto |
